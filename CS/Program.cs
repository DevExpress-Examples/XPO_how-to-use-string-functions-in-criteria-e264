using System;
using System.Collections.Generic;

namespace UpperFunction {
    static class Program {
        /// <summary>
        /// The main entry point for the application.
        /// </summary>
        [STAThread]
        static void Main() {
            new UpperFunction().Run();
        }
    }
}